# Production dependencies for the Songs API Lambda function
# These packages are required for running the API in AWS Lambda
# - boto3: AWS SDK for Python
# - python-dotenv: Environment variable management

# Test dependencies
pytest==8.1.1
pytest-cov==4.1.0
moto[dynamodb]==5.0.3
requests==2.31.0

boto3==1.34.34
python-dotenv==1.0.1 